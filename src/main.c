#include <stdio.h>

int main(){
    
    char *input; // user input on the shell

    while (1)
    {
        /* store the user input and check  */
        printf("$");
        scanf("%s", input);

        printf("%s\n", input);
        

    }
    

    return 0;
}